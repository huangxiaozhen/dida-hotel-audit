[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dida-hotel-audit"
version = "0.1.0"
description = "Authenticated gateway and Agent Skill for Dida hotel static-content audits"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
dida-hotel-audit-gateway = "dida_hotel_audit.cli:main"

[tool.setuptools]
packages = ["dida_hotel_audit"]
